import { Logger } from '../../utils/Logger.js';

export async function runCommand(_options: unknown): Promise<void> {
  const logger = new Logger();
  logger.info('Run command not yet implemented');
}