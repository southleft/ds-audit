import { Logger } from '../../utils/Logger.js';

export async function configCommand(_options: unknown): Promise<void> {
  const logger = new Logger();
  logger.info('Config command not yet implemented');
}